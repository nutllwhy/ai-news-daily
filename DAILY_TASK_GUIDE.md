# AI 日报深度生成任务指南 (V3)

本指南用于指导自动化 Agent 生成高质量、深度内容、报纸风格的 AI 日报。

## 🎯 任务目标
- **深度内容**：不再是简单的摘要，要有背景、数据、分析、影响。
- **报纸风格**：使用 V3 版 HTML 模板。
- **纯文本简报**：生成无 Markdown 的微信风格简报。

## 🛠️ 执行步骤

### Step 1: 深度搜索 (Search & Fetch)
1. 使用 `web_search` 搜索今日热门 AI 新闻（Google, OpenAI, Anthropic, DeepSeek 等）。
2. **关键步骤**：对于筛选出的 4-5 条重要新闻，**必须使用 `web_fetch` 获取网页正文**。
   - 不要只看搜索结果的 snippet。
   - 要阅读全文，提取：具体数据（股价跌幅、参数量）、引用观点（CEO言论、分析师评论）、背景信息。

### Step 2: 撰写深度文案 (Write)
基于 Fetch 到的内容，撰写新闻稿。
- **头版头条**：300字以上。包含：事件 -> 影响 -> 数据 -> 深度结论。
- **行业动态**：150字以上。包含：产品核心价值 -> 与竞品对比 -> 解决了什么痛点。
- **今日数据**：从新闻中提取 3-4 个关键数字（如 "$1T", "+282%"）。

### Step 3: 构造数据文件 (JSON)
将撰写好的内容构造成 JSON 文件，保存为 `daily_data.json`。格式如下：

```json
{
  "date_str": "2026年2月5日 星期四 | 农历腊月十八",
  "date_full": "2026年2月5日 星期四",
  "issue_no": "第 006 期",
  "headline": {
    "title": "大标题",
    "subtitle": "副标题",
    "time": "10小时前",
    "content": "<p>第一段...</p><h3>小标题</h3><p>第二段...</p>", 
    "sources": [
      {"name": "Bloomberg", "url": "..."}
    ]
  },
  "news_list": [
    {
      "icon": "🏢",
      "category": "大厂动态",
      "title": "标题",
      "meta": "Google Blog | 10小时前",
      "body": "<p>正文...</p>",
      "source_name": "Google",
      "source_url": "..."
    }
  ],
  "stats": [
    {"label": "标签", "value": "数值", "color": "#c41e3a"}
  ],
  "review": [
    "<strong>2月4日</strong><br>Anthropic引发软件股崩盘"
  ]
}
```
*注意：content 和 body 中可以使用 HTML 标签（p, strong, ul, li, h3）。*

### Step 4: 渲染 HTML (Render)
运行渲染脚本：
```bash
python3 /root/.openclaw/workspace/ai-news-daily/render_daily.py daily_data.json
```
此命令会生成 `YYYY-MM-DD.html` 和 `index.html`。

### Step 5: 生成简报 (Brief)
根据 JSON 内容，生成一份**纯文本**简报。
- **禁止 Markdown**（不要 **, ##, []）。
- 格式参考：
```text
【2026-02-05 栗噔噔AI早报】

各位早上好！...

🚨 头版头条
1. 标题
内容...

💬 行业声音
2. 标题
内容...

完整版见链接 👉 https://nutllwhy.github.io/ai-news-daily/index.html
```
保存为 `wechat_brief.txt`。

### Step 6: 发布 (Publish)
1. Git commit & push `ai-news-daily` 目录。
2. 使用 `sessions_send` 将 `wechat_brief.txt` 的内容发送给用户。
